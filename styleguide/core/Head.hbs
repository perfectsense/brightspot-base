{{#defineBlock "Head"}}
    {{#defineElement "styles" noWith=true}}
        <link rel="stylesheet" type="text/css" href="/styleguide/All.min.css">
    {{/defineElement}}

    {{#defineElement "meta" noWith=true}}
        <meta charset="UTF-8">

        {{#if title}}<title>{{title}}</title>{{/if}}
        {{#if canonicalLink}}<link rel="canonical" href="{{canonicalLink}}">{{/if}}
        {{#if description}}<meta name="description" content="{{description}}" />{{/if}}
        {{#if keywords}}<meta name="keywords" content="{{keywords}}" />{{/if}}

        {{#each items}}
            {{this}}
        {{/each}}
    {{/defineElement}}

    {{#defineElement "scripts" noWith=true}}
        <script type="application/javascript" src="/styleguide/All.min.js"></script>
    {{/defineElement}}

    {{#defineBlockContainer}}
        {{#defineBlockBody}}
            {{element "styles"}}
            {{element "meta"}}
            {{element "scripts"}}
        {{/defineBlockBody}}
    {{/defineBlockContainer}}
{{/defineBlock}}
