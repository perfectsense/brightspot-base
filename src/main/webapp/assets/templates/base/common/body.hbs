<body{{#if modifierClass}} class="{{modifierClass}}"{{/if}}>
    <div class="site-wrapper">

        {{#with headerView}}
            {{render this}}
        {{/with}}

        <div class="site" style="{{inlineStyle}}">

            {{render mainView}}

        </div>

        {{#with footerView}}
            {{render this}}
        {{/with}}

    </div>
</body>