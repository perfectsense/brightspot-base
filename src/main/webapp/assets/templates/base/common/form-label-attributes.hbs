{{!--
    This template exists because form labels can be placed before or after inputs and are placed AROUND the input element.
    This way, we can render the <label> as part of the form input hbs and only use this to render the attributes in a BE friendly way.
--}}
{{extraAttributes}}