<div class="grid-container{{#if displayOptions.modifierClass}} {{modifierClass}}{{/if}}">

    {{#each aboveColumns}}
        {{render this}}
    {{/each}}

	<div class="row">

		<div class="column-scrolling">
            {{#each main}}
                {{render this}}
            {{/each}}
		</div>

		<div class="column-fixed-wrap" data-bsp-toggle-item>
			<div class="column-fixed toggle-item">

				<div class="trigger-fixed-column toggle-trigger">
					<a class="toggle-trigger-link-out icon icon-angle-up" href="#">{{#if aside.openText}}{{this}}{{else}}Open{{/if}}</a>
					<a class="toggle-trigger-link-in  icon icon-angle-down" href="#">{{#if aside.closeText}}{{this}}{{else}}Close{{/if}}</a>
				</div>

                {{#each aside}}
                    {{render this}}
                {{/each}}
			</div>
		</div>

	</div>
</div>
