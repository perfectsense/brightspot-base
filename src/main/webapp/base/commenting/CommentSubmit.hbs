{{#defineBlock "CommentSubmit"}}
    {{#defineElement "button"}}
        <button type="button" class="{{elementName}}" {{#if ../ajaxTarget}}data-ajax-target="{{../ajaxTarget}}"{{/if}}>{{this}}</button>
    {{/defineElement}}

    {{#defineBlockContainer}}
        <div class="{{blockName}}"{{extraAttributes}}>
            {{#defineBlockBody}}
                {{element "button"}}
            {{/defineBlockBody}}
        </div>
    {{/defineBlockContainer}}
{{/defineBlock}}
