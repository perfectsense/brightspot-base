<div class="bsp-component">
	{{#if heading}}
		<div class="bsp-component-title">{{heading}}</div>
	{{/if}}

	<div class="bsp-component-content">

		<div class="bsp-image-promo">

		{{#if options.horizontalSplit}}
			<div class="bsp-row">
		{{/if}}

			{{#each images}}

				<div class="bsp-column-{{../options.horizontalSplit}}">

					<div class="bsp-image-promo-image">

						{{#with image}}
							{{> (lookup . '_template') }}
						{{/with}}

						<ul class="bsp-image-promo-links">
							{{#each promoLinks}}
								<li>{{> (lookup . '_template') }}</li>
							{{/each}}
						</ul>
					</div>

				</div>

			{{/each}}

		{{#if options.horizontalSplit}}
			</div>
		{{/if}}

		</div>

	</div>

	{{#if ctaText}}
		<div class="bsp-component-cta">
			<a href="{{ctaLink}}">{{ctaText}}</a>
		</div>	
	{{/if}}	
</div>