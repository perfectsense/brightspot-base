<div class="bsp-component">
	<div class="bsp-site-paginate">
		<ul>
			{{#with navigate}}
				<li class="bsp-site-paginate-navigate">
					{{#with previous}}
						{{> (lookup . '_template') }}
					{{/with}}
				</li>
			{{/with}}
			{{#each listItems}}
				<li class="bsp-site-paginate-page">
				{{#with link}}
					{{> (lookup . '_template') }}
				{{/with}}
				</li>
			{{/each}}
			{{#with navigate}}
				<li class="bsp-site-paginate-navigate">
					{{#with next}}
						{{> (lookup . '_template') }}
					{{/with}}
				</li>
			{{/with}}
		</ul>
	</div>
</div>