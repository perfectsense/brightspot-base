<div class="bsp-tags">
    {{#if this.title}}<div class="bsp-tag-list-title">{{render this.title}}</div>{{/if}}
    <ul class="bsp-tag-list">
        {{#each this.tagList}}
            <li>{{render this cssClass="bsp-tag"}}</li>
        {{/each}}
    </ul>
</div>
