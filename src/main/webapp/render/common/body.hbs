<div class="bsp-site-wrapper{{#if modifierClass}} {{modifierClass}}{{/if}}">

    {{#with headerView}}
        {{render this}}
    {{/with}}

    <div class="bsp-site" style="{{inlineStyle}}">

        {{render mainView}}

    </div>

    {{#with footerView}}
        {{render this}}
    {{/with}}

</div>
